package main

const (
	task = `
Lerne die 2 Linux Befehle cd und ls kennen. Navigiere durch die Ordner bis
du den Schatz findest.
`

	ls = `
ls steht für list und zeigt den Inhalt eines Verzeichnisses bzw. Ordners an. 
Damit ist es einer der am häufigsten verwendeten Befehle auf der Kommandozeile.

Die in diesem Beispiel zu nutzende Syntax von ls ist:

	ls [VERZEICHNIS] 

Wird kein Verzeichnis angeben, so wird der Inhalt des aktuellen Verzeichnisses aufgelistet.
`

	cd = `
cd steht für change directory und dient - unter Berücksichtigung von
Groß-/Kleinschreibung! - zum Wechsel in ein (Unter-)Verzeichnis.

Die in diesem Beispiel zu nutzende Syntax ist:

	cd [VERZEICHNIS]

Wird kein Verzeichnis angegeben, so wird in den $HOME Ordner des Benutzerst gewechselt.
`
)
